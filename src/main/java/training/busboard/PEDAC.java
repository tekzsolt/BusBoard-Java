package training.busboard;

public class PEDAC {
    // 1. Create user input => get stop point id
        // 1.1 Create a User class with prompt() method
        // 1.2 Return stop point id (String)
    // 2. Use the stop point id to search on the TFL API
        // 2.1 Work out what link should be targeted (include stop point id)
    // 3. Get a response from TFL API => JSON
        // 3.1 Map the returned JSON data to the relevant object (arrivals ?)
            // 3.1.1 In what data structure JSON is returned ?
            // 3.1.2 Have to map only certain data, ignore the others
    // 4. Print off the next five buses with extra info
}
